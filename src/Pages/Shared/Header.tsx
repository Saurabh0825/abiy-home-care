import Grid from '@mui/material/Grid2';
import { Typography } from '@mui/material';

const Header = () => {
    return (
        <div>
            <Grid>
                <Typography>
                    Header
                </Typography>
            </Grid>
        </div>
    );
}

export default Header;